<!DOCTYPE html>
<html>
  <head>
    <title>Reservas de Vuelos</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.min.js"></script>
  </head>

  <body>
    <div class="container">
      <h1>Reservas de Vuelos</h1>

      <% if (reservas.length === 0) { %>
      <p>No hay reservas registradas.</p>
      <% } else { %>
      <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Fecha de Reserva</th>
            <th>Fecha Solicitada</th>
            <th>Costo del Viaje</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i = 0; i < reservas.length; i++) { %>
          <tr>
            <td><%= reservas[i].idReserva %></td>
            <td><%= reservas[i].Nombre %></td>
            <td><%= reservas[i].Apellido %></td>
            <td><%= reservas[i].fechaReserva %></td>
            <td><%= reservas[i].fechaSolicitada %></td>
            <td><%= reservas[i].costoViaje %></td>
            <td>
              <a
                href="/api/reservas/editar/<%= reservas[i].idReserva %>"
                class="btn btn-primary"
                >Editar</a
              >
              <a
                href="/api/reservas/eliminar/<%= reservas[i].idReserva %>"
                class="btn btn-danger"
                >Eliminar</a
              >
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
      <% } %>

      <h2>Agregar Reserva</h2>
      <a href="/api/reservas/crear" class="btn btn-success"
        >Crear una nueva reserva</a
      >
    </div>
  </body>
</html>
